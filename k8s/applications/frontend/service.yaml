---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: llm-analytics
  labels:
    app: frontend
    component: ui
spec:
  type: ClusterIP
  selector:
    app: frontend
    component: ui
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
